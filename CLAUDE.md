# CLAUDE 시스템 가이드라인

이 문서는 본 코드베이스 전체에서 AI(Claude) 개발을 위한 **최상위 규칙**이다.  
아래 지침은 모든 코드 생성, 파일 수정, 리팩토링의 기준이 된다.

---

## 📌 1. 프로젝트 문서 사용 규칙

- 모든 작업은 `/docs/PROJECT.md` 내용을 최우선 기준으로 한다.
- 사람이 명시적으로 시키지 않는 한, 규칙을 임의로 변경하거나 확장하지 않는다.

---

## 📌 2. AI 개발자 주석 규칙 (AIDEV-\*)

### 허용된 주석 접두어:

- `AIDEV-NOTE:` → 해당 로직 설명, 제약, 이유 등을 기록
- `AIDEV-TODO:` → 향후 해야 할 작업 (자동 삭제 금지)
- `AIDEV-QUESTION:` → AI가 사람에게 확인이 필요한 지점

### 규칙:

1. 코드를 수정하기 전에 해당 파일 내의 기존 AIDEV-\* 주석이 있는지 **반드시 먼저 검색**한다.
2. 관련 코드를 수정할 때는 **AIDEV-\* 주석도 함께 업데이트**해야 한다.
3. 사람의 명시적 요청 없이 AIDEV-NOTE를 **삭제하면 안 된다**.
4. AIDEV-\* 주석은 검색을 고려하여 **짧고 명확하게** 유지한다.

---

## 📌 3. 금지 규칙 (절대 어기면 안 됨)

- 프로젝트 문서들의 아키텍처 규칙을 무시하는 코드 생성 금지
- TypeScript에서 `any` 사용 금지
- React에서 `useEffect`를 통한 데이터 fetch 금지
- Server Component와 Client Component 경계를 흐리는 코드 생성 금지
- 승인 없이 글로벌 상태(store) 생성 금지

---

## 📌 4. 코드 생성 원칙

- 파일은 항상 **정확한 경로**에 생성한다.
- 함수·컴포넌트는 “직관적이고 간결하며 사이드 이펙트 없는 구조”로 작성한다.
- 실제 존재하지 않는 API나 문법을 만들어내면 안 된다.
- 설명 요청이 아닌 경우 “설명만 있는 답변”이 아니라 **실제 실행 가능한 코드**를 우선 제공한다.

---

## 📌 5. 작동 방식

코드를 요청받으면 다음 순서를 따른다:

1. CLAUDE.md 규칙 검토
2. `/docs/` 내 문서 검토
3. 기존 파일 구조 분석
4. 필요한 경우 AIDEV-\* 주석 추가
5. 코드 생성

## 기능 체크 규칙

Claude는 코드를 생성하거나 수정할 때 `/docs/PROJECT.md`의 체크박스를 기반으로
해당 작업이 어떤 기능 요구사항과 연결되는지 먼저 분석한 뒤,
작업 후 구현 여부를 체크해야 한다.
누락된 기능이 있으면 수정안을 반드시 제시한다.
